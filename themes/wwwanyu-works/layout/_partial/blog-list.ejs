<main class="container-fluid archive">
  <div class="d-flex align-content-start flex-wrap gap-3">
    <% if (pagination ==2){ %>
    <% page.posts.each(function(post){ %>
    <%- partial('article', {post: post, index: true}) %>
    <% }) %>
    <% } else { %>
    <% var lastMonth; %>
    <% page.posts.each(function(post, i){ %>
    <% var month = post.date.format('MMMM YYYY'); %>
    <% if (lastMonth != month){ %>
    <% lastMonth = month; %>
    <% } %>

    <%- partial('blog-cover', {post: post, even: i % 2 == 0}) %>
    <% }) %>
    </ul>
    <% } %>
  </div>
</main>